import {StyleSheet} from 'react-native';

export const Card = StyleSheet.create({
  card: {
    borderWidth: 0,
    backgroundColor: '#FFF',
    borderRadius: 10,
    elevation: 5,
    shadowColor: '#000',
    paddingVertical: 5,
    paddingHorizontal: 10,
  },
});
